
public class placeShip {

	
	/*public static void placeShip(String shipName,int c1,int c2,String direction ){
	
		
		if(shipName==ship.NAME_OF_HELM&&direction=="vertical")
			{for (int i=0;i<ship.SIZE_OF_HELM;i++){
				if(Client.gameBoard[(c1+i)-1][c2-1]==Gameboard.UnoccupiedSpace)
				Client.gameBoard[(c1+i)-1][c2-1]=Gameboard.OccupiedSpace;}}
		if(shipName==ship.NAME_OF_HELM&&direction!="vertical")
			{for (int i=0;i<ship.SIZE_OF_HELM;i++){
				if(Client.gameBoard[c1-1][(c2+i)-1]==Gameboard.UnoccupiedSpace)
				Client.gameBoard[c1-1][(c2+i)-1]=Gameboard.OccupiedSpace;}}
		
		if(shipName==ship.NAME_OF_FOOT&&direction=="vertical")
		{for (int i=0;i<ship.SIZE_OF_FOOT;i++){
			if(Client.gameBoard[(c1+i)-1][c2-1]==Gameboard.UnoccupiedSpace)
			Client.gameBoard[(c1+i)-1][c2-1]=Gameboard.OccupiedSpace;}}
	if(shipName==ship.NAME_OF_FOOT&&direction!="vertical")
		{for (int i=0;i<ship.SIZE_OF_FOOT;i++){
			if(Client.gameBoard[c1-1][(c2+i)-1]==Gameboard.UnoccupiedSpace)
			Client.gameBoard[c1-1][(c2+i)-1]=Gameboard.OccupiedSpace;}}
	
	
	if(shipName==ship.NAME_OF_DINGY&&direction=="vertical")
	{for (int i=0;i<ship.SIZE_OF_DINGY;i++){
		if(Client.gameBoard[(c1+i)-1][c2-1]==Gameboard.UnoccupiedSpace)
		Client.gameBoard[(c1+i)-1][c2-1]=Gameboard.OccupiedSpace;}}
if(shipName==ship.NAME_OF_DINGY&&direction!="vertical")
	{for (int i=0;i<ship.SIZE_OF_DINGY;i++){
		if(Client.gameBoard[c1-1][(c2+i)-1]==Gameboard.UnoccupiedSpace)
		Client.gameBoard[c1-1][(c2+i)-1]=Gameboard.OccupiedSpace;}}



if(shipName==ship.NAME_OF_DESTROYER&&direction=="vertical")
{for (int i=0;i<ship.SIZE_OF_DESTROYER;i++){
	if(Client.gameBoard[(c1+i)-1][c2-1]==Gameboard.UnoccupiedSpace)
	Client.gameBoard[(c1+i)-1][c2-1]=Gameboard.OccupiedSpace;}}
if(shipName==ship.NAME_OF_DESTROYER&&direction!="vertical")
{for (int i=0;i<ship.SIZE_OF_DESTROYER;i++){
	if(Client.gameBoard[c1-1][(c2+i)-1]==Gameboard.UnoccupiedSpace)
	Client.gameBoard[c1-1][(c2+i)-1]=Gameboard.OccupiedSpace;}}
	
	
if(shipName==ship.NAME_OF_CRUISER&&direction=="vertical")
{for (int i=0;i<ship.SIZE_OF_CRUISER;i++){
	if(Client.gameBoard[(c1+i)-1][c2-1]==Gameboard.UnoccupiedSpace)
	Client.gameBoard[(c1+i)-1][c2-1]=Gameboard.OccupiedSpace;}}
if(shipName==ship.NAME_OF_CRUISER&&direction!="vertical")
{for (int i=0;i<ship.SIZE_OF_CRUISER;i++){
	if(Client.gameBoard[c1-1][(c2+i)-1]==Gameboard.UnoccupiedSpace)
	Client.gameBoard[c1-1][(c2+i)-1]=Gameboard.OccupiedSpace;}}
	
	}*/

	
		
		
		/*
		for (int i=0;i<ship.SIZE_OF_HELM;i++){
		if(Client.gameBoard[i][3]==Gameboard.UnoccupiedSpace)
		Client.gameBoard[i][3]=Gameboard.OccupiedSpace;}
	
		for (int i=0;i<ship.SIZE_OF_DESTROYER;i++){
			if(Client.gameBoard[i+3][9]==Gameboard.UnoccupiedSpace)
			Client.gameBoard[i+3][9]=Gameboard.OccupiedSpace;}
		for (int i=0;i<ship.SIZE_OF_CRUISER;i++){
			if(Client.gameBoard[4][i+1]==Gameboard.UnoccupiedSpace)
			Client.gameBoard[4][i+1]=Gameboard.OccupiedSpace;
		}	
		for (int i=0;i<ship.SIZE_OF_FOOT;i++){
			if(Client.gameBoard[7][i+3]==Gameboard.UnoccupiedSpace)
			Client.gameBoard[7][i+3]=Gameboard.OccupiedSpace;
		}
		for (int i=0;i<ship.SIZE_OF_DINGY;i++){
			if(Client.gameBoard[3][i+2]==Gameboard.UnoccupiedSpace)
			Client.gameBoard[3][i+2]=Gameboard.OccupiedSpace;
		}*/
		
		
	}


